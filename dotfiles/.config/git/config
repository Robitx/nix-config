[user]
	name = Tibor Schmidt
	email = robitx@gmail.com

[push]
	default = simple
	autoSetupRemote = true

[safe]
	directory = *

[alias]
	nah = !git reset --hard && git clean -df
	wdiff = diff --color=always --word-diff

[includeIf "gitdir:/backup/szngit/"]
	path = config.szn
